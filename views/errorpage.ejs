<main class="flex-fill d-flex align-items-center justify-content-center text-center">
        <div class="container">
            <h1 class="fw-bold text-primary"><%= errorCode  %></h1>
            <p class="lead mb-4">Sorry, something went wrong</p>

                <a href="/dashboard" class="btn btn-primary btn-lg">
                    Back to Home
                </a>
        </div>
    </main>
